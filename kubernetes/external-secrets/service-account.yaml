---
# Service Account for External Secrets Operator in prod namespace
apiVersion: v1
kind: ServiceAccount
metadata:
  name: external-secrets-sa
  namespace: prod
  labels:
    app.kubernetes.io/name: agentprovision
    app.kubernetes.io/component: secrets
  annotations:
    # Workload Identity binding to GCP Service Account
    iam.gke.io/gcp-service-account: external-secrets@ai-agency-479516.iam.gserviceaccount.com
---
# Service Account for External Secrets Operator in database namespace
apiVersion: v1
kind: ServiceAccount
metadata:
  name: external-secrets-sa
  namespace: database
  labels:
    app.kubernetes.io/name: agentprovision
    app.kubernetes.io/component: secrets
  annotations:
    iam.gke.io/gcp-service-account: external-secrets@ai-agency-479516.iam.gserviceaccount.com
