# Work log — 2026-05-25 to 2026-06-03

**Generated:** 2026-06-03 from git history

46 PRs merged to `main` between 2026-05-25 and 2026-06-03, confirmed via
`gh pr list --state merged --search "merged:2026-05-25..2026-06-04"`. Grouped by
theme; each entry is `#PR — what — why`. Design/backfill docs linked per theme.

---

## Vet OS vertical

Design: [`docs/plans/2026-05-30-vet-practice-provisioner-plan.md`](2026-05-30-vet-practice-provisioner-plan.md),
[`docs/plans/2026-05-30-agentic-vet-os-vision.md`](2026-05-30-agentic-vet-os-vision.md),
[`docs/plans/2026-05-30-veterinary-mvp-discovery.md`](2026-05-30-veterinary-mvp-discovery.md)

- **#740** — vet-practice tenant provisioner v1 (`cardiology_v1` fleet) — one-call seed of a vet practice tenant + agent fleet so onboarding is reproducible, not hand-built.
- **#739** — vet practice-OS landing at `vet.agentprovision.com` — vertical-branded entry point for the veterinary go-to-market.

---

## Code-worker interactive Claude (auth + submission + freeze-recovery)

Design: [`docs/plans/2026-05-29-claude-code-subscription-worker-auth-backfill.md`](2026-05-29-claude-code-subscription-worker-auth-backfill.md) (auth),
[`docs/plans/2026-05-30-claude-code-interactive-prompt-submission.md`](2026-05-30-claude-code-interactive-prompt-submission.md) (submission),
[`docs/plans/2026-06-01-claude-interactive-longtask-watchdog-fix.md`](2026-06-01-claude-interactive-longtask-watchdog-fix.md) (#762 long-task watchdog)

- **#731** — support interactive Claude Code auth — subscription tokens can't drive `claude -p`; the worker must log in via interactive PTY against a native HOME.
- **#732** — pre-complete Claude onboarding for interactive TTY HOME — skip the first-run onboarding wizard so the PTY session reaches a prompt unattended.
- **#733** — web `/integrations` connect via native `claude auth login` — setup-token is blocked for subscription accounts, so the connect flow uses the native login that drops `.credentials.json` into the worker volume.
- **#734** — route subscription Claude chat through native worker auth (per-tenant) — chat turns for a subscription tenant run the interactive PTY with the worker's native credential instead of `-p` + API key.
- **#735** — don't `/exit` interactive Claude before first output — premature exit was killing the session before the answer landed.
- **#736** — submit interactive Claude prompt + read clean answer — actually send the turn and parse a clean response out of the TTY transcript.
- **#738** — auto-accept edits on interactive Claude — unblock the answer Write by auto-accepting the edit-permission prompt.
- **#741** — glob per-turn scratch dir for interactive Claude answer — locate the answer file when its name varies per turn.
- **#742** — submit interactive Claude trigger under startup-chrome flood — make the submit survive the noisy startup-chrome output burst.
- **#743** — recover interactive `claude_code` from startup-freeze under host load — detect and recover when the session freezes during startup on a loaded host.
- **#744** — make freeze-recovery robust to prompt-glyph residue — stop leftover prompt glyphs from defeating the freeze-recovery heuristic.
- **#762** — stop interactive Claude killing long tool turns (pull repos → exit 143) — long tool turns were being SIGTERM'd (exit 143); keep them alive to completion.

---

## GitHub auth (OAuth + SSH keys)

Design: [`docs/plans/2026-05-31-github-ssh-key-support.md`](2026-05-31-github-ssh-key-support.md),
[`docs/plans/2026-05-31-code-worker-git-failfast-and-token.md`](2026-05-31-code-worker-git-failfast-and-token.md)

- **#745** — wire tenant GitHub OAuth into clones + git fail-fast — the worker fetched the OAuth token but never told git to use it, causing a 25-min hang; now wired with fail-fast.
- **#746** — kill the git PAGER hang + unify github auth on `gh` (all CLIs) — interactive pager blocked headless git; standardize all CLIs on `gh` auth.
- **#747** — SSH key support for GitHub — PR1 backend (store / fetch / fingerprint) — let tenants register SSH keys for git over SSH.
- **#748** — SSH key worker wiring — PR2 (ephemeral keyfile + `GIT_SSH_COMMAND`) — materialize the key into a short-lived file and point git at it.
- **#749** — SSH PR2 hardening — exception-safe cleanup + scrub keyfile path — guarantee the ephemeral key is removed even on failure.
- **#750** — GitHub SSH key UI — PR3 (`/integrations` paste field) — surface key registration in the integrations page.

---

## WhatsApp durability + api restart hang

Design: [`docs/plans/2026-06-02-whatsapp-session-durability-design.md`](2026-06-02-whatsapp-session-durability-design.md),
[`docs/plans/2026-06-02-api-shutdown-sse-drain-fix.md`](2026-06-02-api-shutdown-sse-drain-fix.md)

- **#765** — durable validated session persistence + clean-shutdown drain — persist a validated neonize session and drain cleanly on shutdown so restarts no longer force a server-side QR re-pair.
- **#766** — bound uvicorn graceful-shutdown so SSE can't block the WhatsApp drain — open SSE connections were holding the shutdown open and blocking the drain.
- **#767** — close the ~180s restart hang — uvicorn graceful-shutdown via env + `os._exit` past neonize's cgo runtime — force exit past the cgo runtime that refused to release the process.

---

## Core-systems reliability (E2E regressions + Temporal queue)

Design: [`docs/plans/2026-06-01-core-systems-e2e-regression-and-queue-fixes-backfill.md`](2026-06-01-core-systems-e2e-regression-and-queue-fixes-backfill.md)

- **#754** — repair 3 engine data-capture regressions caught by E2E — fix memory/RL capture paths the end-to-end suite flagged as broken.
- **#755** — cap `act_extract_media` retries (unlimited → starved orchestration queue) — an unbounded retry storm was starving the orchestration queue.
- **#757** — kill-switch to stop self-perpetuating monitor loops starving the queue — operator switch to halt runaway monitor workflows.
- **#758** — gate the API monitor auto-launcher on the kill-switch — the auto-launcher was the actual flood source; gate it behind the same switch.
- **#761** — stop ProviderCouncil poison-pill (decode-tolerant input) — malformed input was poison-pilling the review council; make decoding tolerant.

---

## Multi-CLI routing (codex pin, opencode, mcp-auth)

Design: [`docs/plans/2026-06-02-codex-gpt55-model-pin-backfill.md`](2026-06-02-codex-gpt55-model-pin-backfill.md),
[`docs/plans/2026-05-25-agent-fleet-seed-and-cli-routing-fixes-backfill.md`](2026-05-25-agent-fleet-seed-and-cli-routing-fixes-backfill.md) (opencode/mcp-auth)

- **#764** — pin codex model to `gpt-5.5` — codex's default model is rejected by ChatGPT-account auth; pin to a supported model.
- **#711** — opencode: align CLI argv + JSON parser with v1.15.x — restore the last-resort fallback after upstream argv/JSON changes.
- **#717** — opencode: prepend persona prompt to dispatch — stop the "I'm OpenCode" identity leak by injecting the agent persona.
- **#716** — mcp-auth: read headers from `request_context.request.headers` — closes a `tier=anonymous` regression where headers weren't being read.

---

## Agent fleet seeding

Design: [`docs/plans/2026-05-25-agent-fleet-seed-and-cli-routing-fixes-backfill.md`](2026-05-25-agent-fleet-seed-and-cli-routing-fixes-backfill.md)

- **#712** — seed Simon's work fleet — Innovus / Integral / Levi (Luna's authorship) — provision the named work-context agent fleets.
- **#710** — expand Luna Supervisor `tool_groups` for full integration control — give the supervisor the tool scope needed to drive all integrations.

---

## Landing redesign

Design: [`docs/plans/2026-06-01-alpha-landing-four-engine-redesign-backfill.md`](2026-06-01-alpha-landing-four-engine-redesign-backfill.md)

- **#751** — redesign alpha landing around the four-engine substrate — reframe the marketing site around the four-CLI-runtime core story.
- **#752** — add missing `reality` i18n label (landing nav showed raw key) — fix a raw i18n key rendering in the redesigned nav.

---

## subprocess → MCP token

Design: [`docs/plans/2026-05-25-subprocess-mcp-agent-token-propagation.md`](2026-05-25-subprocess-mcp-agent-token-propagation.md),
[`docs/plans/2026-05-25-subprocess-mcp-agent-token-phase0-baseline.md`](2026-05-25-subprocess-mcp-agent-token-phase0-baseline.md),
[`docs/plans/2026-05-25-subprocess-mcp-agent-token-phase1-resolution.md`](2026-05-25-subprocess-mcp-agent-token-phase1-resolution.md)

- **#713** — docs(plans) subprocess→MCP `agent_token` propagation — close the chat→tools tier gap — design for propagating the agent-scoped token from chat subprocess into MCP calls.
- **#714** — docs(plans) Phase 0 baseline — subprocess→MCP `agent_token` propagation — baseline assessment of the current propagation path before the change.

---

## Luna Learn from Media

Design: [`docs/plans/2026-05-26-luna-learn-from-media-backfill.md`](2026-05-26-luna-learn-from-media-backfill.md)

- **#726** — Luna Learn from Media — meta-skill for video → skill synthesis (full impl) — Luna can watch a video and synthesize a reusable skill from it.

---

## F2 keychain (runner-context diagnostics + wontfix)

Design: [`docs/plans/2026-05-25-f2-keychain-wontfix-decision.md`](2026-05-25-f2-keychain-wontfix-decision.md)

- **#718** — F2 keychain runner-context diagnostic step (one-shot) — instrument the self-hosted runner to capture keychain behaviour.
- **#719** — F2 keychain diagnostic v2 — capture `security`'s exit code + ACL inspection — deepen the probe with exit codes and ACL state.
- **#720** — `setup-keychain.sh` add `-A` flag (no ACL prompt) — closes runner keychain refusal — avoid the interactive ACL prompt that blocked the runner.
- **#721** — `setup-keychain.sh` delete-then-add for fresh ACL — closes `-U` gap from #720 — rebuild the ACL cleanly to fix the remaining gap.
- **#723** — runner-context keychain probe v3 (TEMP) — temporary deeper probe of the runner keychain context.
- **#724** — disable login-keychain auto-lock + revert diagnostic v3 — try keeping the keychain unlocked and remove the temp probe.
- **#725** — abandon keychain migration — login keychain session-isolated from runner — wontfix: the login keychain is session-isolated from the runner, so the migration is not viable.
